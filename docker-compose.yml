services:
  db:
    image: postgres:15-alpine
    container_name: oracle-chat-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: oracle
      POSTGRES_PASSWORD: oracle
      POSTGRES_DB: oracle_chat
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
